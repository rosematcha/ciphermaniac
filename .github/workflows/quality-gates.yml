name: Quality Gates

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  quality-gates:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Security audit
        run: npm audit --omit=dev --audit-level=high

      - name: Lint
        run: npm run lint

      - name: Knip (dead code)
        run: npm run knip

      - name: Typecheck
        run: npm run typecheck

      - name: Typecheck backend
        run: npm run typecheck:backend

      - name: Typecheck node scripts and tests
        run: npm run typecheck:node

      - name: Build production bundle
        run: npm run build

      - name: Coverage
        run: npm run test:coverage
