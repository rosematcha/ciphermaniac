User-agent: *
Allow: /

# Internal export tool — reachable by link, but not listed on /tools and not
# worth a crawl budget.
Disallow: /tools/social-graphics
Disallow: /toys/social-graphics

# Design system reference for whoever is building the site.
Disallow: /style

# One page per seat per event, and the round files behind them are pruned a few
# days later. Live URLs are for sharing, not for an index.
Disallow: /live/

Sitemap: https://ciphermaniac.com/sitemap.xml
