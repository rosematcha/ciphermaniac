/**
 * Card page entry point
 * @module Card
 *
 * Delegates to src/card/page.ts for bootstrap logic.
 * Structured as src/card/{page,pageState,loader}.ts + existing siblings.
 */

// Side-effect import: runs the card page bootstrap
import './card/page.js';
